# Baseline evaluator contract

The verifier exits nonzero if any accepted Claim 1-3 regression exceeds tolerance, if the negative control does not fail, or if the historical variance proxy no longer produces all 108 finite combinations.

Claims 4-6 remain BLOCKED. The 1D variance proxy is labeled exactly **Historical rejected baseline** and must not appear as the current verifier in a release candidate.
