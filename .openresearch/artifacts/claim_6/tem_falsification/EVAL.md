# Evaluator contract

Current verifier: `python -m repro.check_tem .openresearch/artifacts/claim_6/tem_falsification/result.json` after the fixed command regenerates `result.json`.

The page must expose the exact claim and quantifier, assumptions, both primary archive identities and manifests, CPU allocation and runtime, capability probes, corruption control, four-route record, falsification conclusion, raw JSON, checker output, and limitations. Until those generated outputs are mirrored into the candidate Space, evaluator visibility remains blocked.
