# Rendering route limitations

- This is a capability and protocol audit, not the 768-seed pose experiment.
- GPU hardware is prohibited by campaign policy.
- The cited GenDR implementation requires CUDA and is not the unpublished application implementation used by the paper.
- No substitute CPU renderer is treated as verification evidence.
